# Decision log

Append-only. One dated line per real decision, newest last. Format: `YYYY-MM-DD: decision — reason`.

- 2026-05-02: Predicates, not procedures — goal predicates (now/soon/eventual) name success conditions; the model decides how to satisfy them. Pre-built solution templates are forbidden; emergence is the point.
- 2026-05-03: Skills carry procedural knowledge without specifics — order, dependencies, tradeoffs; never dimensions/materials/coordinates. Same recipe must serve a peasant hut and a royal suite via the model's parameter choices.
- 2026-05-03: Stairwells default 2×2, span many z in one bulk designation, rooms branch beside the spine — user (DF expert) correction; single-column/shallow/hidden-averse advice is wrong on all counts.
- 2026-07-11: Architecture flip approved (Feature 008) — the Go orchestrator becomes an MCP server; Claude Code is the mind (session memory, skills, compaction); the bespoke LLM loop (internal/bdi + internal/llm) is retired after First Fort. Reason: the loop was a hand-rolled amnesiac agent harness; the harness already exists.
- 2026-07-11: Restructure in place on branch `008-mcp-server`; months of uncommitted BDI work snapshot-committed first (1bd95d1) as archaeology.
- 2026-07-11: First Fort is the Phase-1 exit milestone — supervised session: underground shelter, food/drink, beds, survive year 1.
- 2026-07-11: Quickfort/blueprints are a dial, not a rewrite — library blueprints now, model-drafted layouts later through the same validated tool; efficiency of AI-drafted layouts becomes a measurable research question.
- 2026-07-11: Execution process = subagent-driven waves (implement→review→fix gates) orchestrated by workflows; fable for implementation and review, sonnet for transcription-grade tasks.
- 2026-07-11: Target DF 53.15 / DFHack 53.15-r1 (the "dino update") — user-directed upgrade before the API audit so drift is fixed once. Dinosaur content requires newly generated worlds.
- 2026-07-11: Plugin builds via directory junction into the DFHack source checkout + `plugins/CMakeLists.custom.txt` hook — the old copied-directory setup had silently built stale code; there is no DFHack SDK, in-tree is the only supported path.
- 2026-07-12: Command/query/resync queues drain from the socket thread under CoreSuspender — live checkpoint proved `plugin_onupdate` never fires while DF is paused, which deadlocked the whole turn protocol (couldn't even unpause).
- 2026-07-12: Building placement uses `constructWithFilters` with per-type job_item filters (DFHack's own path), and the command dispatcher wraps DF calls in try/catch — `constructAbstract` on a workshop throws, and an uncaught throw off a thread hard-crashed DF.
- 2026-07-12: Perception stays slices + model-side assembly — confirmed by direct play experience; no volumetric/3D text renderings. Queued from the same experience: designation overlays in `look`, an elevation view (third orthogonal plane), and a Go-side reachability dry-run.
- 2026-07-12: `step` counts simulation ticks (1200 = 1 game day); long steps are the intended rhythm, with planned critical-announcement tripwires that end a step early — "let it run and react" over frame-polling.
- 2026-07-12: Cognition stays in-house — no external memory/retrieval MCPs (the learning loop is the experiment); external MCP servers only at presentation boundaries (OBS/Twitch, Phase 3).
- 2026-07-12: Build tool speaks CENTER coordinates for workshops and converts to the protocol's NW-corner wire semantic in one place (`buildWireCoords`) — models think in centers; the wire format doesn't.
- 2026-07-12: Repo adopts the stateless-docs organization (this structure) — CLAUDE.md bootstrap + docs/index + append-only decisions + stateless guides; pre-MCP root docs archived.
- 2026-07-12: Phase briefs authored — specs/009-culture-and-learning (zones port, perception round 2, skills-as-culture, tripwires, homeostasis) gated on First Fort; specs/010-stream-harness (Agent SDK runtime, camera/OBS, chat, channels) gated on 009's learning loop — priorities deliberately reorderable by First Fort observations.
- 2026-07-12: Session-1 feedback wave scoped under 008 (not 009): chop/gather become real Designations::markPlant handlers; stairs designation gains continuation semantics (adjoining carved stairs join the shaft, carved tiles skipped); plugin sends real FortInfo date (cur_year/cur_year_tick; day-of-year + season derived); TILE_UPDATE delta flags fixed to full recompute.
- 2026-07-12: Water perception ships fog-honest: flow_size depth digits + water_table aquifer flags + neighbor-derived damp exposed in map_slice/column_profile/survey INCLUDING hidden tiles — justified because DF's own damp-cancel warnings make aquifers player-knowable; stocks gains material names + economic flag; list_buildings query + remove_building command + build material class added.
- 2026-07-12: Step tripwires pulled forward from 009 §5 into this wave (critical announcement auto-repauses a running step; sim_status carries tripwire reason).
- 2026-07-12: First Fort session 2 fix waves — (a) real chop/gather verified live (3→13 logs); (b) direct workshop job-queue command (queue_job, bypasses the manager/office requirement entirely) unblocked beds and confirmed a second latent bug: manager_order.mat_type=-1 was DFHack's invalid-material sentinel, not "any material" (mat_index=-1 is correct; fixed alongside); (c) stocks/orders/alerts token-efficiency fixes — stocks was already silently truncating mid-JSON at 7-dwarf fort size, orders wasted ~2000 tok/call on a static catalog, alerts had an unexposed Dismiss mechanism (all three fixed Go-only, no plugin change).
- 2026-07-12: First Fort declared sufficient proof-of-concept for the 008→009 gate WITHOUT reaching the runbook's literal year-2-spring survival criterion (fort is at day 103 of year 100, no migrant wave yet) — user call, explicit tradeoff accepted: core loop (dig/build/step/react, real production chains, live crisis handling) is proven; risk is a later migrant wave or crisis revealing a new problem class after 009 work has already started. Fort continues in the background/periodically rather than as primary focus. Deferred-not-blocking backlog from this session (fortress/memory/goals.md): BrewDrink + hatch-cover job_type mappings, generalized item construction (supersedes one-off ORDER_TYPE_* additions), set_labor tool (field-confirmed critical — single miner blocked by fish-cleaning labor collision), dwarf/animal census filter, find_dig_site stale-solidity bug, designation+building overlays in look (now TWO independent live incidents of the room/shaft-gap error class), stocks category case-sensitivity.
- 2026-07-12: find_dig_site stale-solidity backlog item closed — confirmed the plugin-side fix already on this branch (062455f, `tile_updates.cpp`'s TILE_UPDATE delta path computing real flags via `compute_tile_flags()` instead of hardcoding `FLAG_DISCOVERED`) is the correct and complete root-cause fix; the Go consumer chain (`ClassifyState`/`TopologyOverlay`/`mapview.FindDigSites`) was already correct given honest wire flags, so no Go source change was needed. Added two Go regression tests (`internal/worldmodel/populator_test.go`) exercising `Populator.OnTileUpdate` → `FindDigSites` end-to-end with a fixture room: one locks in current (fixed) behavior, one pins the pre-fix failure mode itself (degraded flags in → false-solid candidate out) as a tripwire against the Go side ever reintroducing a placeholder/default flags value on some future ingestion path. The actual bug's code (the hardcoded flags line) had no Go-testable pure function, hence no test was added at the plugin layer.
- 2026-07-12: Generalized item construction shipped for `queue_job` — the hand-maintained ORDER_TYPE_* whitelist (protocolToJobType, ~11 entries) is now bypassable via a name-based path: QUEUE_JOB's wire payload gained an optional trailing length-prefixed job-type name (sentinel byte ORDER_TYPE_BY_NAME=0x00, mirrors BLUEPRINT's name tail), resolved plugin-side via DFHack's `find_enum_item<df::job_type>` (the same mechanism DFHack's own built-in `orders` plugin uses) — no C++ addition needed for any job_type DFHack already knows about, unblocking the hatch-cover gap noted above. Deliberately NOT generalized this pass (confirmed underivable from DFHack enum metadata — no `workshop` attribute exists on job_type, no material-class attribute on the furniture/barrel/meal jobs): `jobTypeAllowedAtWorkshop`'s workshop-compatibility table and the WOOD/BOULDER material filter stay hand-maintained; a resolved-by-name job_type can still be rejected by that whitelist (e.g. ConstructHatchCover resolves cleanly but isn't listed yet). `job_types` MCP tool added as an on-demand, filterable discovery lookup (not folded into orders/queue_job, keeping those cheap on every call) — WORK_ORDER/`order` intentionally out of scope, still byte-vocabulary-only.
- 2026-07-12: Pre-009 blocking-fixes wave closes the last two goals.md backlog items — `set_labor` (new SET_LABOR wire command; a direct `df::unit.status.labors[]` toggle bounds-checked against LABOR_MAX_INDEX, the same mechanism DFHack's own autolabor plugin uses, no manager/queue involved — addresses the field-confirmed single-miner/fish-cleaning labor collision) and the dwarf/animal census fix (`dfhack-plugin/entities.cpp` was classifying via `Units::isFortControlled()`, which is true for any tame unit; a new `Units::isAnimal()` check now splits `ENTITY_TYPE_ANIMAL` off before the citizen/fort-controlled branch, so pets and pack animals stop inflating `dwarves`/`check_goals`' per-dwarf math). Both are Go-build-clean and locked in by new regression tests (`internal/predicate/library_test.go`, `internal/worldmodel/populator_test.go`); unlike the generalized-construction and find_dig_site fixes logged above, neither has been exercised against a live fort yet — goals.md's backlog checkboxes for both stay open pending that verification.
- 2026-07-12: Controller fix on top of the pre-009 blocking-fixes wave — `handleListOrders` (`dfhack-plugin/queries.cpp`, the handler backing the new `job_types` discovery tool) held a dangling-pointer bug the wave's own final reviewer caught and correctly scoped out as "pre-existing, not introduced by this diff": `const char *name = ENUM_KEY_STR(...).c_str()` took a pointer into a temporary `std::string` destroyed at the end of the statement, then dereferenced it on every subsequent line. Harmless while dead code; live risk now that `job_types` makes this path load-bearing. Fixed by owning the `std::string` instead of a raw pointer into it — no behavior change, plugin rebuilt clean.
- 2026-07-12: Region graph (Feature 009 sub-project 1, Task 1) vertical adjacency stays permissive (any open tile stacked on another counts), not the design doc's literal "matching stair tiles" — review caught the implementation-vs-doc mismatch, and the wire protocol has no spare bits/fields to carry tile-shape sub-type (`TileState.Flags` is full; `FLAG_FLOOR` already conflates floor/ramp/stair), so a real fix means an additive protocol field plus a plugin rebuild, out of scope for a Task 1 fix pass. `design-perception-round2.md` Component 1 amended in place to document the deviation and its accepted false-positive risk (ordinary floor-over-floor stacks report as one region; false positives only, never false negatives) instead of silently drifting from the doc it's supposed to implement. `internal/topology/regions_test.go` pins the behavior explicitly (`TestRegionGraph_VerticalAdjacencyIsPermissive`) so a future stair-shape-aware tightening is a deliberate change, not a silent regression discovery. Not scheduled; revisit if live play surfaces an actual false-positive incident.
- 2026-07-13: Zones' assignment-mechanism table (`design-zones.md` Component 3) refined post-merge — Dormitory moved from "unconfirmed" to the roster mechanism, and MeetingHall/ArcheryRange/Dungeon/AnimalTraining get specific, accurate error text instead of the generic "not confirmed" bucket. Rationale: re-reading `Buildings::setOwner` and the roster write path (`general_ref_building_civzone_assignedst` + `assigned_units`) directly found zero type restriction anywhere in DFHack's own code — the Pen/Pond-only gate lives entirely in `plugins/zone.cpp`'s own `assignUnitToZone()` guard clause, not a DFHack API or struct-level limitation. The earlier "6 confirmed types" framing conflated "what DFHack's own reference plugins happen to use" with "what the API permits". Dormitory's *write* is proven-safe (identical shipped roster code path) but its in-game *effect* is unverified, same confidence tier as `assign_lodging`. MeetingHall (real mechanism: DF's Location system, not civzone ownership), ArcheryRange/Dungeon (squad-keyed via `squad_room_info`/`Military::updateRoomAssignments`, same as Barracks), and AnimalTraining (labor-driven, no assignment record) were traced to their real non-assignment mechanisms rather than left as "unconfirmed". `dfhack-plugin/zones.cpp` rebuilt clean; the remaining 6 zone types (WaterSource, Dump, SandCollection, FishingArea, ClayCollection, PlantGathering) stay genuinely unconfirmed, unchanged.
- 2026-07-14: First Fort round 2 (fresh embark) live-verified the Zones+Locations tool families end-to-end and — the session's real experiment — pierced and sealed a single-layer sand aquifer FULLY UNSUPERVISED using only fortress/memory/learnings.md's written protocol (~11 game-days, day 18→29; walls up day 27-29, residual puddle evaporated by day 38). Protocol enrichments discovered: ring digs can need THREE designation rounds with zero alert feedback (announcement dedup pools repeats), and quarrying stone BEFORE opening the ring beats the written order. assign_lodging's wire path verified (list_locations shows the guest room); its in-game visitor effect is pending seasons/wealth — deliberately left open, not glossed as success.
- 2026-07-14: Ultracode improvement wave (7 scouts/researchers → 3 implementation lanes with per-task Fable review gates, Sonnet implementers) shipped in one day, all compile/test-verified only (live DF was running; DLL deploy deferred): (a) reaction-based queue_job (ORDER_TYPE_CUSTOM_REACTION=0x0D + reaction code tail; job_items cloned from reaction reagents per DFHack's own workshops.lua recipe incl. flags1-5 and fuel; workshop compat DERIVED from reaction->building parallel arrays — unlike job types, reactions carry their own compat data, so no hand table) + list_reactions discovery — unblocks brewing/soap/dye; (b) farm plots as dedicated extent-shaped commands (BUILD_FARM_PLOT=0x16, SET_FARM_CROP=0x17 — NOT a build-tool type: DFHack groups FarmPlot with stockpiles/zones, needsItems()==false forces an empty-filters constructWithFilters path the shared placeBuilding helper rejects) + per-season assign_crop (incl. fallow/all) + list_crops; (c) remove_zone (0x15; rejects Location-founding zones with a named error pending verified cascade semantics); (d) announcement repeat_count wired end-to-end (DF pools repeated announcements in-place; the id-cursor made every repeat invisible — step reports now say "repeated warnings this step: ... xN"; decode is length-tolerant for old-DLL compat); (e) FLAG_DESIGNATED now includes tiles claimed by in-flight dig jobs (DF stops reflecting the designation bit once a job claims the tile — dig-now.cpp's job-list technique, applied in BOTH full-state and delta paths); (f) per-column-honest SURFACE semantics (the old marker stamped the global max-dwarf-z onto any queried column) + survey_site reports a surface RANGE (hills above spawn are normal, workable ground); (g) look radius cap 15→23 + scope=overview (downsampled whole-map view from the resident topology overlay, zero plugin round-trips); (h) connector-suggestion fixes (diagonal suggestions would have designated bounding-box rectangles; vertical adjacency was invisible to the connectivity check); (i) dwarf_detail render + include_labors opt-in and dwarves verbose census mode; (j) ConstructHatchCover whitelist entry (closes the Fort #1 hatch gap). ALL of it needs live verification next session after a DLL deploy.
- 2026-07-14: First skill library established at .claude/skills/ — aquifer-piercing (light-verified protocol; heavy aquifers explicitly marked UNVERIFIED with community approaches listed for orientation only; identification-gate-first structure), df-farming (full seed economy incl. the cooking-destroys-seeds trap; kitchen-permissions tool named as the missing mitigation), fort-opening (embark gate checklist incl. the confirmed-universal single-miner pattern), and aquifer-water-infrastructure as a PLANNED stub (blocked on well/floodgate/lever tooling — the "PLANNED —" description prefix is the convention for tooling-blocked skills). House rule enforced by review gate: procedural knowledge only, no dimensions/materials; the 2x2 stair shaft remains the sole sanctioned specific. Promotion pipeline decided: tool-fix > tool-description > skill (needs 2-fort confirmation for procedures) > learnings.md (accumulator for provisional lessons).
- 2026-07-15: Fort-planning capability wave (2 scouts + 2 Fable researchers -> 3 review-gated lanes): (a) look gains scope=elevation (full z-level, four stitched 48x48 slices Go-side — deliberately NOT a plugin cap raise: shorter CoreSuspender holds, no DLL redeploy; ~1 tok/tile, auto 2x2 modal-GLYPH downsample above ~100-wide with header disclosure — material classes survive, unlike overview's 3-state collapse) and scope=fort (modified-footprint bbox + 8-tile margin from the resident ModificationOverlay — the token-optimal routine planning view; truthful "nothing recorded on z=N" when empty); (b) blueprint system repaired and completed: quickfort stair aliases were silently failing on apply (parseQuickfortGrid emits updown_stair/down_stair/up_stair, digTypeFromName only knew stairs/downstair/upstair — every shipped CSV's stairwell tiles no-oped; remove_ramp confirmed to have NO wire dig-type, now a named error), apply coalesces contiguous same-row runs into single SendDigRegion calls (was 1 wire round trip PER TILE — 700+ per pod), library re-scans blueprints/*.csv before every lookup (was load-once at process start), and the orphaned CreateBlueprintFromModifications/SaveToCSV pair is finally wired as save_blueprint + list_blueprints — the playing AI can dig a pod once, capture it, and re-stamp it to scale with migrant waves; (c) fort-planning skill authored from on-disk Dreamfort analysis + community research: level-category system (two hard constraints — farming in top soil, industry in first stone — rest site-flexible), main 2x2 spine as surface-path-only + satellite stairs per cluster, dig-everything-early/furnish-by-population-gates, starter-then-permanent rooms, defense staging ladder with the overseer's highway+pit+bridge end-state recorded (earthworks diggable now, seal blocked on bridge/lever tooling), v50 facts encoded (cave-in pillars are decorative; migrant waves 2 hardcoded then wealth-driven). Overseer confirmed all four taste defaults live: undug-plug interim seal (miner-as-lever), hybrid cluster topology (loops for industry, dead-ends for housing), rough-dig-next-level dig-ahead with idle-miner smoothing, bilateral-symmetry aesthetic canon + soft pop cap 60-80.
- 2026-07-15: Fort #3 crash root-caused — stockpile tool sets settings.flags only, leaving raws-indexed item vectors size-0 (DF UI reads them unchecked). Fix approach chosen: after constructAbstract, invoke the stockpiles plugin's exported Lua fn (Lua::CallLuaModuleFunction "stockpiles"/"stockpiles_import") with bundled .dfstock presets instead of reimplementing ~500 lines of raws sizing. Tool quarantined until it ships. Also confirmed: set_labor's status.labors[] write is the correct v50 mechanism (work details are UI-only); second-miner ceiling is pick supply.
- 2026-07-15: Fix wave (ultracode, 19 agents) shipped + DLL deployed: stockpile settings via stockpiles_import/everything.dfstock (PARTIAL ACK degrade path), brewing reaction catalog, assign_crop (under-construction + stamp-time-wall tiles), forge/smelter/trade-depot build types (5x5 depot center->NW), scope=fort player-mods-only bbox, blueprint capture under buildings, connector-hint stairs adjacency, remove_zone severity, case-insensitive filters. All compile/test-verified; live verification pending next fort session.
- 2026-07-15: Steam auto-updated DFHack to 53.15-r2 mid-day; the r1-built plugin refused to load (exact version-string rule). Retargeted the dfhack-build checkout to 53.15-r2 (+submodules), rebuilt clean (exit 0, no codegen flake), redeployed with checksum match. Session-start rule reinforced: verify the plugin loads before diagnosing anything else.
- 2026-07-15: Defense-systems wave launched (ultracode): burrows + v50 civilian-alert tools and mechanisms chain (lever/bridge/floodgate build types, mechanism crafting, link_building, pull_lever) — functionality only; skills for mechanism/drawbridge design come after in-game constructability is confirmed. Caravan doctrine recorded: early caravans are pack animals (stairs OK); wagon caravans need an excavated ramp road to an in-fort depot — plan into whole-map layouts later.
- 2026-07-15: Defense-systems wave SHIPPED + DLL deployed (23:19): burrows (designate/remove/assign/unassign/list) + set_alert vanilla civilian alert; lever/floodgate/bridge build types (bridge WxH+direction, 31x31 cap); link_building (2 mechanisms, bridge/floodgate/door/hatch targets); pull_lever. Review gates caught 2 serious defects pre-ship: assign-before-alert doctrine was backwards (alert affects ALL civilians unassigned — assignment would linger and starve dwarves) and link_building ignored fallible moveToBuilding returns (SUCCESS ack on corrupted half-link). Both fixed. Compile/test-verified; live verification next session.
- 2026-07-16: BUG FOUND LIVE (Fort #4 session 2): a `designate_dig type=mine` room designation that overlaps an existing carved 2x2 up/down stair shaft silently strips the up-stair component on every overlapped tile, converting `UpDownStair` to plain `DownStair` — the shaft's connection to everything above is severed with no ACK warning. Re-issuing `designate_dig type=stairs` on the same tiles afterward does NOT fix it: the plugin's designation path checks "is this tile already carved" as a boolean and skips re-queuing (ACK: "already carved", 0 designated) regardless of stair sub-type, so there is currently NO in-game repair path once this happens via re-designation. `build` does have registered stair types (`upstair`/`downstair`/`updownstair` in `internal/mcpserver/tools_action.go:227-228`, `protocol.BuildType{Up,Down,UpDown}Stair`) but whether `build type=updownstair` can actually construct a stair ON an already-open floor tile (vs. only being wired for some other placement path) is UNVERIFIED — untested this session; the user is fixing the live tile directly and asked for this to be logged rather than have me experiment further mid-incident. RESOLVED same session: `build type=updownstair` (registered name, not "stairs") DOES work as a real repair path — tested on a virgin floor tile first (converted cleanly to a functioning `X` up/down stair), then applied to the 2 actually-broken core tiles (the other 2 of the 4 self-healed via the earlier stairs re-designation's queued job finishing on its own). Full shaft connectivity confirmed restored via cross_section top-to-bottom. TODO (1) below was implemented then DELIBERATELY REVERSED the same day — see the 2026-07-16 "round 2" entry further down for why. (2) `build type=updownstair` onto an already-carved DownStair tile (rather than virgin floor) is still unconfirmed as exactly the code path that fired, vs. a coincidental adjacent effect — low priority, the repair path works regardless.
- 2026-07-16: Fix wave round 1 (workflow, 4 bugs from the session-2 incidents above) shipped and build-verified (Go: 244 tests/34 packages; C++ plugin: clean rebuild against the real DFHack checkout, artifact not deployed): (a) `designate_dig type=mine`/`channel`/`ramp` made to SKIP already-carved stair tiles instead of silently destroying them; (b) `stocks` split into free `count` + new `in_use` field; (c) `look`/`cross_section` gained a `smoothed` annotation (riding the existing DAMP/AQUIFER text-annotation channel, deliberately not touching the full binary tile-flags byte); (d) `remove_building` now scans for overlapping Stockpiles (invisible to `Buildings::findAtTile` since stockpiles are abstract, non-occupancy-setting buildings) and returns a disambiguation error instead of guessing. Adversarial review (same workflow) found real follow-up issues in 3 of 4: (a) was TOO restrictive — mining over a stair is legitimate, intentional DF functionality (removing/repurposing a stairwell, no different from vanilla), and the fix design of the current owner "user-directed" review immediately caught it; (b) `min_count` filter silently dropped 100%-built-in entries; (c) `isSmoothedAt` false-positived on every player-built Construction tile (DF stamps the same `SMOOTH` special value on constructed walls/floors for an unrelated reason — confirmed via `df.d_basics.xml`), meaning built aquifer-seal walls would misreport as smoothed; (d) `remove_building`'s new disambiguation error was technically correct but gave unsatisfiable advice when a stockpile fully encloses a building (no hole-punching support means every tile of the enclosed building is "ambiguous," and reissuing elsewhere is impossible from that side).
- 2026-07-16: Fix wave round 2 — user-directed reversal of (a) above plus fixes for (b)/(c)/(d), all re-verified (Go: 247 tests/34 packages; C++ plugin: clean incremental rebuild) and adversarially re-reviewed to a clean PASS on all four. Reasoning for the reversal, explicitly: the original round-1 fix traded one silent failure mode (destroys a stair with zero warning) for another (silently preserves a stair no matter what the caller intended, indistinguishable from a real "already carved" skip) — both fail this project's own "truthful ACK" standard, and blanket-protecting stairs contradicts the established `CLAUDE.md` philosophy (see the "hidden tiles are diggable by design" gotcha — over-eager validation that blocks legitimate actions was this project's founding bug, and stairs are just a dig shape, not a constructed building with a deconstruct command). Final design: `designate_dig type=mine`/`channel`/`ramp` once again CAN convert an already-carved stair tile to plain floor (matching vanilla DF's own stair-removal mechanic) — but now increments a distinct `convertedStairs` counter and reports it explicitly and separately in the ACK ("N will remove existing stairs: vertical connection lost"), never conflated with the unrelated `skippedCarved`/`promotedCarved` stairs-on-stairs counters. A one-line gotcha was added to `CLAUDE.md` advising a look/cross_section check before designating a footprint that might contain a stair shaft, since the tool will no longer prevent the mistake — nudge, not gate. `min_count` now surfaces any entry with real `in_use` presence regardless of free-stock threshold; `isSmoothedAt` gained a `tileMaterial(tt) != CONSTRUCTION` guard; `remove_building`'s disambiguation error now spells out the two-step stockpile-first-then-building workaround when both a stockpile and a real building are candidates.
- 2026-07-16: BUG FOUND LIVE — `stocks` counts an item type's total across BOTH loose/available stock AND items already incorporated into built furniture (e.g. `stocks category=bed` reported 7 while all 7 were already built into rooms, zero were actually placeable spares). This caused repeated silent "needs bed"/"needs door" cancels on new construction that looked, from the stocks output alone, like they should have had material. `stocks` should report only free/haulable stock; "what's already placed as a building" is `buildings`' job, and the two views are currently conflated in one count. Fix: exclude items with a non-null incorporated/building reference from `stocks`' tally, or add a separate "in-use" column so the two are distinguishable at a glance without cross-referencing `buildings` by hand.
- 2026-07-16: BUG FOUND LIVE — `look` never reflects `smooth` having succeeded (a smoothed wall renders identically to an unsmoothed one in the ASCII overlay); confirmed via direct in-game observation that a `smooth mode=wall` call had actually taken effect while `look`/`cross_section` gave no visual signal either way. Smoothing status should be a queryable/renderable state, same class of gap as the existing designation-overlay under-reporting issue.
- 2026-07-16: Adversarial review caught a performance regression in the just-added `floor_items` annotation (`map_slice`/`column_profile`'s new loose-item-count field): `floorItemCounts()` did an unbounded scan of `world->items.all` on every call, on the two hottest per-turn perception tools, multiplied further by `look scope=elevation/fort`'s multi-slice stitching and `elevation_view`'s up-to-30 `column_profile` RPCs per call. Fixed by reusing DFHack's own per-16x16-block lazy item-count cache idiom (the same one backing `MapExtras::Block::itemCountAt`, confirmed via `MapCache.cpp`'s `init_item_counts`): `floorItemCountAt()` now scans only a touched block's own `map_block::items` on first visit, memoized per query call, never the whole fort. Kept the existing `in_building`/`construction` exclusion filter — confirmed via `gui/autodump.lua`'s identical exclusion in the DFHack scripts tree that DF's on-ground bookkeeping does NOT already drop those, so `itemCountAt`'s raw count alone would over-report.
- 2026-07-17: Fort #4 session 3 live-verified all 4 of the 2026-07-16 round-2 fixes (stairs ACK, stocks free/in_use, smooth annotation + Construction guard, remove_building disambiguation) plus the defense-systems mechanism chain (lever/bridge/link_building/pull_lever) end-to-end for the first time — build mechanic workshop → craft mechanisms → build lever+bridge over a `designate_dig type=channel` gap (a flat-ground bridge attempt fails cleanly with no gap present, confirmed live) → link → pull, twice, no errors. `remove_building`'s disambiguation branch itself stayed unexercised (the one stockpile+workshop overlap tile available resolved cleanly to the workshop with no ambiguity — DF's own stockpile room-extents bitmask apparently already excludes tiles a later-built workshop occupies) but destroyed the fort's actual Still as a side effect of picking that test tile; rebuilt same session. Follow-up same day extended the mechanism check past bridge to the full `link_building` target set (protocol caps it at exactly 4: bridge/door/hatch/floodgate, confirmed from `protocol.h` constants, no well/trap/restraint exists anywhere) — door and hatch both verified, including multi-target linking (one lever driving three separate mechanisms at once, `linked_mechanisms` is a vector). Floodgate could not be tested: `job_type::ConstructFloodgate` was completely absent from `jobTypeAllowedAtWorkshop`'s hand-maintained switch (`work_orders.cpp`), rejected at every workshop type including the two that are actually correct (Carpenters/Masons, same pairing as `ConstructDoor`/`ConstructHatchCover` right next to it). Fixed same session by adding the missing case — genuinely one line, since the WOOD/BOULDER material filter downstream is keyed by workshop type, not job type, so no second change site existed. Not yet rebuilt/deployed/live-verified. Incidentally narrowed (not solved) the long-standing "needs non-economic logs" mystery: the material filter code explicitly sets `job_item::flags2.bits.non_economic = true` on every queued furniture-class job, and a `ConstructDoor` job that failed against 12 free willow logs succeeded immediately after chopping one fresh non-willow tree — consistent with willow specifically being economic-flagged in this world, though the actual flag source is still unconfirmed.
- 2026-07-17: Docs reorg for cheaper session onboarding — superseded speckit specs 001-007, the executed 2026-07-12/13 plan artifacts, the First Fort runbook, and the obsolete standalone-build plugin README archived under docs/archive/ via git mv (pointers left: specs/README.md, fortress/SESSION-SETUP.md, new dfhack-plugin/README.md); dead bedroom_analysis.json removed; aquifer-water-infrastructure skill re-statused to the live-verified mechanism-chain reality.
- 2026-07-17: BUG FOUND LIVE — döbar starved to death in a pit and `dwarves`/`dwarf_detail` kept reporting her as a normal idle unit at an unchanged position across multiple polls; DF's own unit never leaves `units.active` on death, and `entities.cpp`'s `isFortControlled()` classification branch (which a dead citizen falls into once `isCitizen()`'s sanity check excludes her) doesn't check death at all, so she stayed stamped ENTITY_TYPE_DWARF forever. Fix: surface DFHack's own `Units::isDead()` (`flags2.bits.killed || flags3.bits.ghostly`, confirmed against this checkout's `library/modules/Units.cpp`, DFHack 53.15-r2) as a `dead` field on `dwarf_detail`'s JSON (trivial, that response was already free-form) and as a `[DEAD]` tag on `dwarves`' compact roster — the latter required a new additive wire block (`DeadUnits`, unit-id list) appended to `ENTITY_UPDATE` after the existing `Zones` block, same backward-compatible pattern (old decoders never read the trailing bytes; no version bump). Classification itself is untouched — a dead dwarf is still, correctly, a dwarf. While wiring the round-trip test, found and fixed a latent asymmetry in the Go-side `serializeEntityUpdate` test encoder (wrote `HasZones=0` when zones were empty, but unconditionally still emitted the 4-byte zone count after it, desyncing anything appended later) — harmless before because Zones was the last block; the real C++ producer already always writes `HasZones=1` unconditionally, so production traffic was never affected.
- 2026-07-18: Water/power infrastructure wave shipped ScrewPump/GearAssembly/AxleHorizontal/AxleVertical/WaterWheel/Windmill/Rollers (new `build` types 0xA0-0xA6, `resolveCuratedBuildTypeByte` mappings, `building_types` catalog entries) plus a new COMMAND_TYPE_BUILD trailing `Orientation` byte (BUILD_ORIENT_*, third optional backward-compatible byte after Material/Quality) reused by name-path builds too. Applied the tool-schema house rule for the first time: the 7 new curated names went into `buildTypes`/`buildingTypeCatalog` (kept the existing 1:1-with-catalog invariant) but were deliberately NOT appended to the `type` param's already-long pipe-enumerated jsonschema string — same precedent the room-value-furniture family already set (discoverable via `building_types`, not schema text). Bridge's width/height/direction shape was checked and does NOT fit: these 7 types anchor at one tile, with DF computing the footprint from building type + orientation (`Buildings::getCorrectSize`), not a caller-chosen rectangle — so orientation became one more optional trailing byte on the existing single-tile BUILD wire instead of a new command. One real limitation found and documented rather than silently shipped wrong: AxleHorizontal/Rollers are the one sub-family where DF's own `getCorrectSize` DOES let the caller choose a length (like Bridge/FarmPlot), but this wave's wire has no length parameter for them — they always build 1 tile long; a longer line needs multiple adjacent placements (mechanically equivalent via DF's own machine adjacency, costs more material than a native multi-tile drag). MakeChain/Well were already wired by an earlier same-day pass — nothing to do there. Adjacency between machine parts (which tile connects to which) is DF's own engine-level concern once pieces exist and is not modeled or verified by this plugin at placement time, for any of the 7.
- 2026-07-18: Added ArcheryTarget/TractionBench/NestBox/Hive (new `build` types 0x9A-0x9D, all forced 1x1 by `Buildings::getCorrectSize`'s default branch). ArcheryTarget got its own placer (`placeArcheryTarget`, generic building-material filter, accepts `material=`) since it doesn't share the "specific item / tool-use, no material knob" shape of the other three; TractionBench/NestBox/Hive were folded into the existing `placeRoomValueFurniture` dispatch purely as code reuse — none of the three is actually DF "room-value" furniture, the doc comments say so explicitly to avoid future confusion. Kept applying the tool-schema house rule: all four went into `buildTypes`/`buildingTypeCatalog` (satisfying the existing 1:1 test invariant) but were NOT appended to the `type` param's jsonschema enum text. Real fix found while wiring TractionBench: `workshops.lua`'s Mechanics job list for `ConstructTractionBench` names a reagent `item_type=df.item_type.MECHANISM`, which does not exist in this DFHack build (`df/item_type.h` has no MECHANISM entry at all) — resolved to `TRAPPARTS`, the same item_type/vector_id every other "mechanism" reagent in this codebase already uses (GearAssembly/Rollers/Well, `ConstructMechanisms`'s own output). `ConstructTractionBench` needed a bespoke 3-job_item (TABLE+TRAPPARTS+CHAIN) special case in `applyQueueJob`, since its generic single-filter path only ever builds one job_item; added to `jobTypeAllowedAtWorkshop` as Mechanics-only. `MakeChain` was already wired (2026-07-18 earlier same-day pass) — confirmed, not re-added. Scope boundary held: no Cage/Chain building_type, no MakeCage, no CageTrap — all justice/restraint-adjacent and explicitly out of scope. NestBox/Hive placement works against any existing has_tool_use-capable TOOL item; crafting that specific tool item is a separate, pre-existing, still-unsolved gap (`queue_job`/`MakeTool` job_items have no parameter to pick an item_subtype), documented as such rather than attempted.
- 2026-07-18: Added Kiln/GlassFurnace/MagmaSmelter/MagmaGlassFurnace/MagmaKiln (new `build` types 0x72-0x76, `df::building_type::Furnace` subtypes, all forced 3x3) plus MagmaForge (new type 0x1A). Source check overturned the task's own premise: MagmaForge is a `df::workshop_type` value (confirmed against `df.building.xml` — it sits inside the `workshop_type` enum-type block, not the neighboring `furnace_type` block), i.e. a Workshop subtype exactly like MetalsmithsForge, NOT a Furnace subtype despite the name — placed via `placeWorkshop`, not `placeFurnace`. Added a `makeMagmaSafeBuildMatFilter`/`makeMagmaAnvilFilter` pair (magma_safe flag instead of fire_safe, per `buildings.lua` furnace_inputs 207-209 and workshop_inputs' MagmaForge entry 229-237) alongside the existing fire-safe helpers; Kiln/GlassFurnace reuse the existing fire-safe filter unchanged (buildings.lua already grouped them with WoodFurnace/Smelter). Magma-placement validation investigated and found to be a non-issue to solve: DFHack's own construction path (`checkFreeTiles`/`getCorrectSize`) has no magma-adjacency check at all for any furnace subtype — its only liquid-related gate is the generic "reject actively-flowing liquid tiles" rule every actual building already gets — so a magma furnace/forge builds successfully on ordinary dry floor with no real magma access; whether it can then actually be WORKED is a live-game runtime question outside DFHack's (and this plugin's) placement-time visibility, documented as a known limitation rather than papered over with invented validation. Followed the tool-schema house rule precedent again: all 6 new names went into `buildTypes`/`buildingTypeCatalog` (1:1 test invariant preserved) but were not appended to the `type` param's jsonschema text.
- 2026-07-18: Labor control will move to work-detail-layer tools — live play + source research (DFHack 53.15-r2) proved `set_labor`'s `unit.status.labors` write targets a layer v50 derives FROM `plotinfo->labor_info.work_details` (DF's own recompute `Units::setAutomaticProfessions` is authoritative; the manipulator precedent our code cites is tagged unavailable-in-v50). Two labors (mining, woodcutting) confirmed detail-gated live: fort owned 2 picks + an axe the whole time while `set_labor`-enabled dwarves never worked. Planned surface: `work_details` read, `assign_work_detail` (membership edit + engine recompute), `set_work_detail_mode`, `create_work_detail`; the autolabor-style global bypass flag is rejected because it disables the vanilla Work Details UI the human co-player actively uses.
- 2026-07-18: Manager work orders (`order`) are PARTIALLY trustworthy — CORRECTED after longer observation. PrepareMeal verified genuinely end-to-end (dispatched, completed, count decremented, 18 real meals in stocks). But MakeFigurine and SmeltOre emitted blank-material "X (N) has been completed" announcements while producing NO item, never decrementing the order, and (figurine) never even dispatching per the orders list — the phantom "completions" initially fooled a whole session's bookkeeping (and explain the "figurine didn't credit the mandate" mystery: nothing was made). ConstructBlocks never dispatched at all. Play-level rule until root-caused: treat order-path completion alerts as untrustworthy without a stocks check; `queue_job` direct-at-workshop remains the only verified production path. Consequence: SmeltOre (unwired in queue_job pending material pinning) is currently UNREACHABLE — the iron arc is tooling-blocked with ore+fuel+furnaces all ready. Root-cause (order conditions/material fields? dispatch validation? announcement misattribution?) promoted into the next engineering wave alongside work-details and trade.
- 2026-07-19: Root-caused and fixed the manager-work-order path — `applyWorkOrder` (`work_orders.cpp`) never set any material selector on a `manager_order` (neither `mat_type`/`mat_index` nor `material_category`), which is why MakeFigurine (material choice also picks the workshop family) and SmeltOre (which ore) never resolved; `order`/`WorkOrderDesignation` gained a `Material` field (a `job_material_category` keyword — wood/bone/shell/leather/silk/plant/cloth/yarn — or an exact DFHack material token via `MaterialInfo::find`, the same mechanism DFHack's own `orders` plugin uses) plus a `Frequency` field (`WorkOrderFrequency*` → `df::workquota_frequency_type`), both additive trailing wire fields (old payloads decode with Material="" and Frequency=OneTime, matching the `BUILD_ORIENT_*` precedent). `queue_job` gained the same `Material` param to unlock a dedicated `SmeltOre` case (job-level `mat_type`/`mat_index` pin + a matching BOULDER job_item + a conditional BAR/COAL fuel item at a plain Smelter, not MagmaSmelter — shape confirmed against `workshops.lua`'s `addSmeltJobs`); `jobTypeAllowedAtFurnace` now allows SmeltOre at both furnace subtypes. MeltMetalObject stays out of scope (needs an `allow_melt_dump`-flagged-item job_item, a structurally different shape — no wire parameter exists to name a specific already-flagged item). Left `manager_order::status.bits.validated = 1` UNCHANGED despite diverging from DFHack's own reference order library (every shipped example has `is_validated:false`) — flagged as a real but unproven divergence needing an isolated live A/B test this pass couldn't run; do not flip it opportunistically alongside other changes. `manager_orders` query gained `material`/`material_category`/`frequency` fields (the `orders` tool renders them) so a session can directly see whether an order actually got material-pinned instead of inferring it from job_type name alone. All compile/test-verified only — DLL redeploy + live test (ore + coal in stock) is unstarted.
- 2026-07-19: Trade/caravan tooling shipped for the safe subset only — research pass traced `df::global::plotinfo->caravans`/`timed_events`/`dip_meeting_info` and `Items::markForTrade` (the exact call `scripts/internal/caravan/movegoods.lua` uses, a pure data-layer op with no viewscreen). New query handlers `caravan_status` (active caravans, trade state, departure countdown, tribute/casualty/etc flags, session value/mood, liaison-meeting flag, scheduled-but-not-arrived `timed_events` entries, depot readiness) and `depot_goods` (staged `contained_items` use_mode=TEMP vs pending `BringItemToDepot` jobs, aggregated by item type + material like `stocks`) plus a new guarded command `COMMAND_TYPE_BRING_GOODS_TO_DEPOT=0x23` (`applyBringGoodsToDepot`: filter-based by item_type/material substring + required max_count cap + optional max_total_value cap, `Maps::canWalkBetween` reachability check, clears `forbid` on mark like `movegoods.lua`'s own `onDismiss`) were all already written and compiling clean by a prior session that died mid-task before wiring any MCP tool — confirmed via source audit against DFHack 53.15-r2 headers (`caravan_state.h`, `building_tradedepotst.h`, `plot_merchant_flag.h`, `timed_event.h`, `meeting_diplomat_info.h`) that every field/signature used matches exactly. This pass's actual work: registered the three MCP tools (`caravan_status`/`depot_goods` in `tools_state.go`, `bring_goods_to_depot` in `tools_action.go`) with shape-only schemas (filter substrings + required max_count, no per-value prose), render functions with unit tests against canned JSON (mirroring the `mandates`/`noble_demands` pattern), and confirmed the wire round-trip tests + guarded dispatch case already existed and pass. Executing the actual trade exchange (the Offer/Trade commit at the depot) stays explicitly OUT OF SCOPE and undocumented-as-a-tool: `trade_interfacest`'s `good`/`goodflag`/`open` state is populated by native, non-exported DF viewscreen input-handling code that DFHack's own most-invasive `trade.lua` never calls either (it only flips `goodflag[...].selected`) — no CHECK_*-free API for the commit step exists anywhere in `Items.h`/`Buildings.h`/`Job.h`, confirmed by source search, not merely asserted. Broker/"anyone can trade" assignment is similarly out of scope (`entity_position_responsibility`'s trade bit was located but not traced to a safe mutation); `caravan_status`'s depot section surfaces the read-only `anyone_can_trade`/`trader_requested` flags as the safe bypass instead. Net capability at the next caravan: a model can see it coming/arrived/leaving and its state, see what's staged vs. pending at the depot, and mark fort goods for hauling there — it cannot itself execute the trade or assign a broker; those remain human-in-the-client actions. All three tools compile/test-verified only (C++ Release build clean, Go build+test clean, `TestToolSchemaBudget` passing at 72 tools/49885 bytes) — DLL redeploy + live verification at an actual caravan is unstarted.
